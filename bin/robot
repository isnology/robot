#!/usr/bin/env ruby

# controller of MVC

require './lib/table'
require './lib/robot'
require './lib/input'

table = Table.new
robot = Robot.new
input = Input.new

loop do
  input.enter_command
  break if input.quit?
  robot.execute_command(input.command, table)
end
